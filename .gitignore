.env
.venv/
__pycache__/
*.pyc
node_modules/
.next/
out/
data/corpus/raw/
# Derived from raw/ + the manifest; ~1GB and fully reproducible by
# `python3 harness/corpus/parse.py`. The manifest itself IS committed.
data/corpus/parsed/
data/corpus/chunks/
data/corpus/embeddings/
data/corpus/.fetch_state.json
# resume position; the golden set itself IS committed
data/golden/.gen_state.json
data/golden/.verify_state.json
*.log
.DS_Store

# wrangler local state (dev cache, tmp bundles, account cache)
.wrangler/
